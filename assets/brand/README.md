# 品牌图标（favicon 与页面角标）

请将站点图标图片放置为本目录下的 `cheatsheet.png`。

- 推荐尺寸：至少 256×256（支持浏览器缩放）。
- 建议格式：PNG（透明背景更佳）。

放置完成后：

- 构建脚本会为所有内容页自动注入 `<link rel="icon" ...>`。
- 导览页模板会自动引用该图标；
- 全站页面（内容页）会在左下角以轻量角标形式展示该图标（不遮挡、不可点击）。

